// Entry point superseded by src/client.tsx (TanStack Start).
// Router registration and bootstrapping are now handled by
// src/router.tsx  →  src/client.tsx  (client hydration)
// src/routes/__root.tsx  (HTML shell)
// src/routes/analysis.tsx, login.tsx, index.tsx  (page routes)
export {}

